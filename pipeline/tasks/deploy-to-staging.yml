---
platform: linux
image: docker:///allomov/cf-cli-only#6.9.0
inputs:
  - name: github-repository
run:
  path: ./github-repository/pipeline/bin/cf-blue-green-deployment
  args:
  - "{{cf-api}}"
  - "{{cf-username}}"
  - "{{cf-password}}"
  - "{{cf-org}}"
  - "staging"
  - "staging-incident-registration"
  - "github-repository"
  - "./github-repository/pipeline/manifests/staging-manifest.yml"
