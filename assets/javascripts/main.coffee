jQuery -> 
  $.backstretch("/images/wall.jpg")
  // clock = $('#clock')
  // currentDate = new Date()
  // meetDate  = new Date(clock.data('date'))
  // diff = meetDate.getTime() / 1000 - currentDate.getTime() / 1000
  // if diff > 0
  //   clock.FlipClock(diff, clockFace: 'DailyCounter', countdown: true)



